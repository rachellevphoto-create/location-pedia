-- This file intentionally left empty. The PostGIS extension is managed by
-- Prisma via `extensions = [postgis]` in schema.prisma, and the spatial index
-- is created by `prisma/postgis-index.sql` (run with `npm run db:postgis`).
SELECT 1;
